{
  "definitions": [
    {
      "title": "Columns Auth",
      "id": "columns-auth",
      "plugins": {
        "xwalk": {
          "page": {
            "resourceType": "core/franklin/components/block/v1/block",
            "template": {
              "name": "Columns Auth",
              "filter": "columns-auth"
            }
          }
        }
      }
    },
    {
      "title": "Image + Content (add only ONE of these)",
      "id": "columns-auth-column",
      "plugins": {
        "xwalk": {
          "page": {
            "resourceType": "core/franklin/components/block/v1/block/item",
            "template": {
              "name": "Column",
              "model": "columns-auth-column"
            }
          }
        }
      }
    }
  ],
  "models": [
    {
      "id": "columns-auth-column",
      "fields": [
        {
          "component": "reference",
          "valueType": "string",
          "name": "image",
          "label": "1. Image (becomes the left column)",
          "multi": false,
          "description": "The promotional photo"
        },
        {
          "component": "richtext",
          "name": "text",
          "value": "",
          "label": "2. Content (becomes the right column)",
          "valueType": "string",
          "description": "Fill this AND Image above — on this SAME item. Do NOT click + to add a second one of these; add only ONE, with both fields filled."
        }
      ]
    }
  ],
  "filters": [
    {
      "id": "columns-auth",
      "components": [
        "columns-auth-column"
      ]
    }
  ]
}
